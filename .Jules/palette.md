## 2026-01-25 - Localized Aria Labels
**Learning:** This app uses a custom `translate` function in `app.js`. When adding `aria-label` to components, ensure the key exists in the `dictionary` object for both 'tr' and 'en'.
**Action:** Check `dictionary` before adding new labels.

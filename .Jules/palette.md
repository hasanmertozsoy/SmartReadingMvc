## 2026-01-26 - Semantic Refactoring of Dashboard Cards
**Learning:** When converting interactive `div` cards to `<button>` for accessibility, explicitly adding `w-full text-left` is crucial to maintain the original grid layout and content alignment.
**Action:** Inspect CSS layout properties of `div`s before converting to `button`s and map them to appropriate utility classes.
